/* 
After gaining a solid understanding of arrays, loops, and if-else statements, I am practicing by creating various tasks to enhance my skills. I plan to write code for a palindrome checker, FizzBuzz, a prime number finder, a number pattern generator, a program to calculate the sum of even or odd numbers, and more. */


// 1- Write a program that checks if a given string or word is a palindrome (a word that reads the same forwards and backwards). Ignore spaces and case sensitivity.

// let userResponse = "madam"

// userResponse = userResponse.toLowerCase()
// let filteredResponse = userResponse.split(" ").join("")
// let isPalindrome = false
// for (let i = 0; i < filteredResponse.length; i++) {
//     if(filteredResponse[i] !== filteredResponse[filteredResponse.length - 1 - i]){
//         isPalindrome = false
//         break
//     }
//     isPalindrome = true

// }
// console.log(isPalindrome);

// 2- Write a program that prints numbers from 1 to 100. For each number:
    // Print "Fizz" if it’s divisible by 3,
    // Print "Buzz" if it’s divisible by 5,
    // Print "FizzBuzz" if it’s divisible by both 3 and 5,
    // Otherwise, print the number itself.


// for (let i = 1; i <= 100; i++) {
//     if (i % 3 === 0 && i % 5 === 0) {
//         console.log("fizzBuzz");
//     } else if (i % 3 === 0) {
//         console.log("fizz");
//     } else if (i % 5 === 0) {
//         console.log("buzz");
//     } else {
//         console.log(i)
//     }
// }

// 3- Write a program that checks if a given number is prime. A prime number is a number greater than 1 that cannot be formed by multiplying two smaller natural numbers.

    // How to do it:
    // Use a loop to check if the given number is divisible by any number other than 1 and itself.
    // If the number is divisible by any other number, it’s not prime.
    // If no divisors are found, it is a prime number.


